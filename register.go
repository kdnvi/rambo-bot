package main

import (
	"github.com/kdnvi/rambo-bot/bot"
)

// registerAll wires up all slash commands and event handlers.
// Populated incrementally as each feature stage is implemented.
func registerAll(b *bot.Bot) {
	// Commands registered in later stages.
	_ = b
}
